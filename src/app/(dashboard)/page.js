import OverviewPage from "@/features/overview/components/OverviewPage";

export const metadata = {
  title: "Overview",
  description:
    "Quality Standards hub for limits and fits, dimensions, threads, GD&T, roughness, and hardness.",
};

const HomePage = () => {
  return <OverviewPage />;
};

export default HomePage;
